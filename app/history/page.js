'use client';import {useEffect,useState} from 'react';
const KEY='qtb_history';export default function History(){const [items,setItems]=useState([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem(KEY)||'[]'))}catch{}} ,[]);function clear(){localStorage.removeItem(KEY);setItems([])}return <main style={{maxWidth:900,margin:'0 auto',padding:40}}><a href="/dashboard">← Dashboard</a><h1>🕘 History</h1><p>Recently used tools on this device.</p>{items.length?<><ul>{items.map((x,i)=><li key={i}><a href={`/tools/${x.slug}`}>{x.title||x.slug}</a></li>)}</ul><button onClick={clear}>Clear history</button></>:<p>Your tool history will appear here.</p>}</main>}
