'use client';import {useEffect,useState} from 'react';
const KEY='qtb_saved';export default function Saved(){const [items,setItems]=useState([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem(KEY)||'[]'))}catch{}} ,[]);function clear(){localStorage.removeItem(KEY);setItems([])}return <main style={{maxWidth:900,margin:'0 auto',padding:40}}><a href="/dashboard">← Dashboard</a><h1>💾 Saved Results</h1><p>Results saved locally in this browser.</p>{items.length?<><div>{items.map((x,i)=><article key={i}><h3>{x.title||'Saved result'}</h3><pre style={{whiteSpace:'pre-wrap'}}>{x.text||JSON.stringify(x,null,2)}</pre></article>)}</div><button onClick={clear}>Clear saved results</button></>:<p>No saved results yet.</p>}</main>}
