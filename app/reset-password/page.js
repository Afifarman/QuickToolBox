'use client';import{useState}from'react';import{createClient}from'../../lib/supabase/client';
export default function ResetPassword(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[sent,setSent]=useState(false);const[msg,setMsg]=useState('');async function send(e){e.preventDefault();const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setMsg(error?.message||'Reset email sent. Check your inbox.');setSent(!error)}async function update(e){e.preventDefault();const{error}=await createClient().auth.updateUser({password});setMsg(error?.message||'Password updated successfully.')}return <main className="auth-page"><div className="auth-card"><a href="/login">← Login</a><h1>Reset Password</h1>{!sent?<form onSubmit={send}><input type="email" placeholder="Your email" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="btn">Send reset link</button></form>:<form onSubmit={update}><input type="password" placeholder="New password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn">Update password</button></form>}{msg&&<p>{msg}</p>}</div></main>}
